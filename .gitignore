.DS_Store
node_modules/
dist/
*.log
.env*

# Penpot — file-ids gerado por export-snapshot.js (contém UUIDs do workspace)
penpot/file-ids.json

# Tokens gerados (rebuild via `pnpm tokens:build`)
packages/tokens/src/
packages/tokens-*/src/

# PDF do manual — referência local apenas, fora do repo
manual_identidade.pdf
public/manual_identidade.pdf

# generated
apps/*-docs/src/build-info.js
